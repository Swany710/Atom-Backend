# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/atom_backend

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here

# Server Configuration
PORT=3000
NODE_ENV=development

# Google API Configuration (for Gmail & Calendar)
# Follow the setup guide in GOOGLE_SETUP_GUIDE.md to obtain these credentials

# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Enable Gmail API and Google Calendar API
# 4. Create OAuth 2.0 credentials
# 5. Download credentials and copy values here

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REFRESH_TOKEN=your-refresh-token

# Note: GOOGLE_REFRESH_TOKEN is obtained after first OAuth flow
# Run the auth endpoint to get it: GET /api/v1/auth/google
